<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.homa.mappers.replyMapper">
	
	<!-- 댓글 조회 -->
	<select id="readReply" resultType="com.homa.domain.ReplyVO"> 
		select
			c_num, c_content, userId, c_date
		from Comments
			where fb_num=#{fb_num}
	</select>
	
	<!-- 댓글 작성 -->
	<insert id="writeReply">
		insert into 
			Comments(fb_num,c_num, c_content, userId)
		 values(#{fb_num},Comments_seq.nextval, #{c_content}, #{userId})
	</insert>
	
	<!-- 특정 댓글 조회 -->
	<select id="readReplySelect" resultType="com.homa.domain.ReplyVO">
		select
			fb_num, c_num, c_content, userId, c_date
		from Comments
			where c_num = #{c_num}
	</select>
	
	<!-- 댓글 수정 -->
	<update id="updateReply">
		update Comments
			set
				c_content = #{c_content}
			where c_num = #{c_num}
	</update>
	
	<!-- 댓글 삭제 -->
	<delete id="deleteReply">
		delete from Comments
			where c_num = #{c_num}
	</delete>
	
</mapper>